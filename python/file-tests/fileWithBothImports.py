import wypp
from wypp import drawingLib
from wypp.drawingLib import *
import localMod

def use(x):
    pass

use(wypp)
use(drawingLib)
use(wypp.Record)
use(drawingLib.Point)
use(Point)
use(wypp.List[int])
